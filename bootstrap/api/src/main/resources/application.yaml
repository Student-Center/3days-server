server:
  port: 8080
  shutdown: graceful
spring:
  profiles:
    active: local
    include:
      - application
      - persistence
---
spring:
  config:
    activate:
      on-profile: local
  docker:
    compose:
      file: ./bootstrap/api/compose.yaml
